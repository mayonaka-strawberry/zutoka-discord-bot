"""engine_alpha: from-scratch resumable ZUTOMAYO CARD engine + AlphaZero training stack.

Fully self-contained package. The only external inputs are the card database
(zutomayo/data/cards.json, read as data) and torch/numpy. Nothing here imports
from the zutomayo package.
"""
